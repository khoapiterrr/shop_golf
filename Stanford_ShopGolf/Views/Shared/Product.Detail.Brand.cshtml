@using Stanford_ShopGolf.ViewModels
@model BrandViewModel
<div class="product-brand">
    @if (Model.ShowImage)
    {
        <img src="@Model.ImagePath" class="brand-image" alt="@Model.Title" />
    }
    else
    {
        <span class="brand-title">@Model.Title</span>
    }
</div>